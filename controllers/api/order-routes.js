const router = require('express').Router();
const { Order } = require('../../models');

//TODO: Post Route to create an Order


//TODO: Put Route for updating the order - Leave for a later task

module.exports = router;